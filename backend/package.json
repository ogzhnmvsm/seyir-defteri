{
  "name": "tiyatro-takip-backend",
  "version": "1.0.0",
  "description": "Backend for scraper operations and suggestions API",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon --watch src -e js --exec node src/index.js",
    "postinstall": "cd ../scraper && npm install && npx puppeteer browsers install chrome"
  },
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "pg": "^8.18.0",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
